#!/usr/bin/env sh

echo "🔍 Running lint..."
npm run -s lint || exit 1

echo "🎨 Checking formatting..."
npm run -s format:check || exit 1

echo "🧪 Running unit tests..."
npm run -s test:run || exit 1

echo "🌐 Running E2E tests..."
npm run -s test:e2e || exit 1

echo "✅ Pre-push checks passed!"
